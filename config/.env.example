# Copy to `config/.env` and fill in your keys (gitignored).
# Layered loading: ambient env > config/.env > <llmn data dir>/.env.
# `export ` prefix, quotes, and `#` comments are supported.

# GEMINI_API_KEY=sk-...
# DEEPSEEK_API_KEY=sk-...
